{
  "name": "nasa-project",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "npm start --prefix server",
    "dev": "npm run dev --prefix server",
    "deploy": "BUILD_PATH=../server/public npm run build --prefix client && npm start --prefix server",
    "deploy-cluster": "set BUILD_PATH=../server/public&& npm run build --prefix client && npm run cluster --prefix server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
